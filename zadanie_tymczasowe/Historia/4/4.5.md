# NEXUS V2 — RAPORT ARCHITEKTONICZNY RUNDA [4.5]

## Faza 4, Krok 4.5 — E2E Chaos Monkey Stress Test (ostateczna weryfikacja)

### AKTUALNY KROK REWIZJI
Chaos Monkey — ostateczny test integracyjny systemu NEXUS V2 w warunkach bojowych. Żadnych mocków, żadnej izolacji — wszystkie podsystemy uruchomione równolegle i poddane ekstremalnemu obciążeniu.

### CO ZOSTAŁO ZROBIONE I DZIAŁA (✅ 1 test E2E na ZIELONO)

| Scenariusz | Status | Szczegóły |
|---|---|---|
| **Pełny bootstrap systemu** | ✅ DZIAŁA | Virtual Workspace, ZCSMAP (10MB), KeyDir, WorkflowRouter, ExecutionRunner, ZombieKiller |
| **500 DAG-ów równolegle (Promise.all)** | ✅ DZIAŁA | 500 agentów DAG uruchomionych jednocześnie |
| **50 agentów z pętlą nieskończoną** | ✅ UBITE PRZEZ CB | `while(true){}` → ExecutionRunner timeout 1500ms → taskkill |
| **10 000 zapisów ZCSMAP** | ✅ DZIAŁA | Concurrent writes do SharedArrayBuffer |
| **EBUSY symulacja** | ✅ DZIAŁA | Plik zablokowany na 300ms w Watched_IO/In |
| **Uszkodzony .jsonl w locie** | ✅ PRZETRWAŁ | Śmieci wstrzyknięte, dobre rekordy nietknięte |
| **Memory leak test** | ✅ PASS | RSS spadł o -3.31 MB (wzrost dozwolony: max +10 MB) |
| **Czas wykonania** | ⚡ 5.8s | Wszystkie operacje w 5.8 sekundy |

### WYNIKI CHAOS MONKEY

```
📊 MEMORY LEAK:
   RSS przed chaosem:    81.03 MB
   RSS po chaosie:       77.72 MB   
   Różnica:              -3.31 MB   ✅ PAMIĘĆ SPADŁA! (GC posprzątał)
   Limit:                <= 10 MB   ✅ PASS

📋 RAPORT CHAOS MONKEY:
   DAG ogółem:       500/500   ✅ wszystkie zakończone
   Ubite przez CB:   50/50     ✅ wszystkie trucizny wykryte i ubite
   Błędy:            0         ✅ zero nieobsłużonych błędów
   ZCSMAP zapisów:   10000     ✅ pełna wydajność bufora
   Czas:             5.8s      ✅
```

### STRUKTURA PLIKÓW NA DYSKU

```
src/backend/
├── e2e/
│   └── chaos.test.ts             (# NOWY 4.5 — E2E Chaos Monkey Stress Test)
├── io/watcherService.ts          (# Phase 4.1)
├── execution/dag.ts              (# Phase 4.2)
├── rag/                          (# Phase 4.3 + 4.4)
│   ├── modelCache.ts
│   ├── onnxRuntime.ts
│   ├── semantic.ts
│   ├── lexical.ts
│   ├── fusion.ts
│   └── index.ts
└── ...
```

### RUNDA [4.5] ZAMKNIĘTA — CHAOS MONKEY PRZESZEDŁ

### PARAMETRYZACJA ARCHITEKTONICZNA — ROZSZERZENIE O CHAOS MONKEY

- **CHAOS_DAG_COUNT** = `500` (liczba współbieżnych DAG-ów)
- **CHAOS_POISON_PERCENTAGE** = `10%` (odsetek agentów z pętlą nieskończoną)
- **CHAOS_ZCSMAP_WRITES** = `10_000` (liczba zapisów w buforze)
- **CHAOS_EBUSY_HOLD_MS** = `300` (czas blokady pliku)
- **CHAOS_TIMEOUT** = `90_000ms` (limit czasu testu)
- **VERDYKT** = Memory leak: -3.31 MB / limit +10 MB. **SYSTEM PRZETRWAŁ**
