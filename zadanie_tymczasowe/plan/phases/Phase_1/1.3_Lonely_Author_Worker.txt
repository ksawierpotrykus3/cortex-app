=========================================
=== PHASE 1 STEP: 1.3_Lonely_Author_Worker.txt ===
=========================================

# NEXUS V2 IMPLEMENTATION - PHASE 1.3
**Moduł:** The Lonely Author Worker Thread
**Cel 0-100%:** Odciążenie wątku Electron V8 poprzez powołanie dedykowanego w 100% pracom dyskowym "Cichego Wątku". Brak interakcji z głównym potokiem zapytań uchroni animacje interfejsu CSS w React przed zatrzymaniami (Jitter) podczas zapisywania 100-megabajtowych zrzutów RAG.

---

## 🛠️ [ZADANIA DLA AI - CODERA]

1. **Środowisko Worker Threads (`src/backend/workers/writer.cjs`)**:
   - Kod wymusza rozszerzenie `.cjs` dla zagwarantowania spójności CommonJS bez udziwnień kompilatorów TypeScript.
   - Wątek obiera wytyczne od procesu głównego używając portu `parentPort.on('message', ...)`. W obiekcie oczekuje referencji do powołanego w kroku 1.2 obiektu pamięci `SharedArrayBuffer` by rzutować na niego własne widoki `Int32Array` i `Uint8Array`.
2. **Sprzętowe Usypianie (Atomics.wait)**:
   - Agenci szturmują tablicę. Samotny autor śpi używając `Atomics.wait(this.meta, 0, lastKnownHead)`. Zamarza on na poziomie jądra OS, nie obciążając wirtualnego rdzenia CPU.
   - Po przebudzeniu odczytuje fizyczny rozmiar z headera. Jeśli długość tekstu rozrywa fizyczną wielkość tablicy, składa obcięte bajty (Split-Char Protection) w jedną ciągłą komórkę zapobiegając uciętym literom "ą".
3. **Cichy Zrzut Dyskowy (Append File)**:
   - Rozpakowuje tablicę binarną za pomocą wbudowanego, najszybszego `new TextDecoder().decode(payloadBytes)`. Kategoryczny zakaz `msgpackr`.
   - Dopisuje wynik do pliku używając asynchronicznej komendy `await fs.promises.appendFile(file, ...)`
   - Po dokonaniu twardego wpisu, podciąga za sobą ogon za pomocą `Atomics.store(this.meta, 1, newTail)`, czym logicznie zwalnia to miejsce dla kolejnych zapisów w głównym wątku. NIE ZWRACA potwierdzenia poprzez `postMessage` – aktualizacja `TAIL` jest jedynym sygnałem sukcesu.

**[WERYFIKACJA DETERMINISTYCZNA (TDD DLA 2026)]**
- **Test 1:** Uruchom instancję MainThread wywołującą 10,000 asynchronicznych modyfikacji i WorkerThread konsumujący wynik. Skrypt musi sprawdzić, czy ani jeden komunikat typu `postMessage` nie został rzucony z wewnątrz Workera do MainThread podczas transferu danych. Oparcie testu o detektor wiadomości IPC udowodni w 100% działanie zasady Zero-Copy na wskaźnikach barierowych.
