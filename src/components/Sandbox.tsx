import React from "react";

export function Sandbox() {
  return (
    <div className="flex flex-col h-full w-full items-center justify-center bg-[rgb(var(--background))] text-[rgb(var(--text-muted))]">
      <p className="text-lg">Laboratorium (Puste)</p>
    </div>
  );
}
